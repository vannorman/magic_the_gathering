before usage:
You need to create a virtualenv and install requirements.
virtualenv magic_venv
source magic_venv/bin/activate
pip install -r requirements.txt

usage:
python print_proxies.py /path/to/deck.txt

todo:
The first time it runs, it will be missing directories and fail. you need to make these directories
don't keep list of saved cards in git


